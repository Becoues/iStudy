# Prisma database URL (relative paths resolved from project root by prisma.ts)
DATABASE_URL="file:./data/istudy.db"

# CometAPI key (https://api.cometapi.com), starts with sk-.
# Optional — the key configured in the in-app settings dialog takes precedence.
COMETAPI_API_KEY=""
